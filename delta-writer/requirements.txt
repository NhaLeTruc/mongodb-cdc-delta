# Delta Writer Service Dependencies
# Python 3.11+

# Delta Lake
deltalake>=0.15.0  # Python bindings for Delta Lake (delta-rs)
pyarrow>=14.0.0    # Arrow format for Delta Lake

# Kafka
kafka-python>=2.0.2       # Kafka client
confluent-kafka>=2.3.0    # Alternative Kafka client with better performance

# AWS S3 / MinIO
aioboto3>=12.0.0         # Async boto3 for S3-compatible storage
boto3>=1.34.0            # AWS SDK for Python

# Async
aiofiles>=23.2.0         # Async file operations
asyncio-mqtt>=0.16.0     # Async MQTT (if needed)

# Logging and monitoring
structlog>=24.1.0        # Structured logging (shared library)
prometheus-client>=0.19.0 # Prometheus metrics

# Tracing
opentelemetry-api>=1.22.0
opentelemetry-sdk>=1.22.0
opentelemetry-instrumentation>=0.43b0
opentelemetry-exporter-jaeger>=1.22.0

# Configuration
pydantic>=2.5.0          # Settings and validation
pydantic-settings>=2.1.0 # Environment-based settings
python-dotenv>=1.0.0     # Load .env files

# Retry and error handling
tenacity>=8.2.3          # Retry with exponential backoff

# Utilities
python-json-logger>=2.0.7  # JSON logging
click>=8.1.7             # CLI interface
